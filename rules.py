# class Rules:
    # returns true if the current position has been repeated five times
    # @staticmethod
    # def is_fivefold_repetition(curr, states) -> bool:
    #     return False
    #     # repetitions = sum(1 for state in states if state == curr)
    #     # return repetitions >= 5

    # @staticmethod
    # def is_seventyfive_moves(move_count) -> bool:
    #     return move_count >= 150

    # @staticmethod
    # def is_fifty_moves(move_count) -> bool:
    #     return move_count >= 100

    # @staticmethod
    # def is_insufficient_material() -> bool:
    #     return False
    #     # king and a king
    #     # king and bishop vs king or (king and (bishop or knight))
    #     # king and knight vs king or (king and (bishop or knight))
    #     # king and two knights